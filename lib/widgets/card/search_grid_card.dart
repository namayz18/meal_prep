import 'package:flutter/material.dart';

class SearchGridCard extends StatelessWidget {
  const SearchGridCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
